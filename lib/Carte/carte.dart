import 'package:flutter/material.dart';

class CartePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Carte'),
      ),
      body: Center(
        child: Text('Welcome to Carte Pageff'),
      ),
    );
  }
}
