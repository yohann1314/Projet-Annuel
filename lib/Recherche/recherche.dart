import 'package:flutter/material.dart';

class RecherchePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recherche'),
      ),
      body: Center(
        child: Text('Welcome to Recherche Pageff'),
      ),
    );
  }
}
